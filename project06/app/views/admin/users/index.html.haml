%h1 Listing users
%p#notice= notice
.paginatewrapper= will_paginate @posts
%table
  %tr
    %th Name
    %th Username
    %th Last Login
    %th FUTURE
  - @users.each do |user|
    %tr
      %td= link_to user.name, edit_admin_user_path(user)
      %td= user.name
      %td= user.username
      - if user.last_login_at
        %td= time_ago_in_words(user.last_login_at) + ' ago'
      - else
        %td none
      %td FUTURE
